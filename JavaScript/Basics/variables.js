// Provide string in double quote
let name="Vishwa"

// Provide number without any quotes
let score=100

console.log(name)
console.log(score)

let firstName="Vishwa "
let lastName="Patel"

// concatinating two strings
console.log(firstName + lastName)

// difference between let and var

// 1) let is block based block means {} if we use under block define value outside of the block then we will get an Error

// 2) var is function based we can use it under defined function can not use it outside of the function


let grade = true;

console.log(grade);